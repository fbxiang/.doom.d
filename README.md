# Doom Emacs Configureation

## Install
``` sh
git clone --depth 1 https://github.com/hlissner/doom-emacs ~/.emacs.d
git clone https://github.com/fbxiang/.doom.d.git ~/.doom.d 
cd ~/.emacs.d
./bin/doom install
```
